---
topic: Mahalla Ovozi UX
updated: 2026-08-11T19:37
---

- (event) Imported user-provided dashboard prototype screenshot as imports/prototype-dashboard-overview-2026-08-05.png.
- (decision) Keep the Hokim dashboard overview-first and neutral: show the complete five-Lane district signal landscape without AI-generated urgency or attention ranking. Make new or recently updated Topics discoverable based on changes since the Hokim last viewed the dashboard.
- (decision) Distinguish Topic change states with explicit Uzbek Cyrillic text labels: Янги for a Topic created since the Hokim last viewed the dashboard, and Янгиланди for an existing Topic that received new evidence or changed information. State meaning must not depend on color alone.
- (decision) Keep Янги and Янгиланди indicators visible throughout the current successful dashboard session. On a later dashboard visit, show indicators only for changes since the preceding successful visit. Do not create per-Topic read, acknowledgement, or completion obligations.
- (decision) Make the entire Topic card the primary control for opening its right-side evidence drawer. Keep Open in Telegram as a separate action on each individual Accepted Evidence item inside the drawer, not as ambiguous Topic-level navigation.
- (decision) The user-provided dashboard prototype is a non-authoritative, fallible reference only. Do not preserve its structure, interactions, content hierarchy, or visual choices by default. Evaluate each element against the PRD, approved UX decisions, accessibility, and user needs; retain, revise, or reject it explicitly.
- (decision) Keep Topic cards compact and do not show a Telegram evidence quote preview on the card. Cards show the cautious Topic summary and required scan metadata; opening the card reveals the complete chronological Accepted Evidence trail in the right-side drawer.
- (decision) When one canonical Topic appears in multiple applicable Lanes, each Topic card shows its additional Lane membership with a short Uzbek Cyrillic textual indicator such as Яна: Ҳокимга оид. Do not use another visually dominant colored badge; opening any appearance leads to the same canonical Topic and evidence drawer.
- (decision) Order Accepted Evidence in the right-side drawer from oldest to newest, with clear source identity and Telegram timestamp, so the Hokim can follow how the situation developed. The Topic card remains the place to see the latest meaningful activity at a glance.
- (decision) On initial dashboard load, order each Lane by latest meaningful activity. During that dashboard session, background refresh preserves visible card positions and Lane scroll. New Topics are added without shifting the current viewport and the fixed Lane header exposes a textual new-item count that can navigate to them; updated Topics stay in place with Янгиланди. A later dashboard visit loads a freshly ordered Lane.
- (decision) While the evidence drawer is open, selecting another Topic card keeps the drawer open and replaces its content with the newly selected canonical Topic. Move the selected-card state to that Topic and preserve every Lane scroll position; do not require a close-and-reopen cycle.
- (decision) The evidence drawer begins with a compact, normally scrolling Topic header containing the cautious summary, Mahalla, all applicable Lanes, latest meaningful activity, and evidence count. The chronological original evidence follows immediately, allowing direct comparison without a permanently sticky summary header.
- (decision) Use a permanently fixed left-to-right Lane order: Ҳокимга оид, Сув, Электр, Газ, Чиқинди. The role-oriented first position makes direct Hokim-related matters easy to find but does not communicate urgency or AI priority; never reorder Lanes dynamically by activity.
- (decision) On the primary desktop layout, keep the compact five-card statistics strip visible while each Lane scrolls independently. Do not auto-hide it or add a manual collapse control in the MVP; preserve the overview-first context with stable layout.
- (decision) Use quick date presets Бугун and Кеча plus one calendar control that supports either a single date or a date range within retained history. Applying any date selection updates all five Lanes and all five statistics together. The calendar control label remains pending explicit microcopy approval.
- (decision) Label the calendar control Сана бўйича with a calendar icon. Before selection it uses that generic label; after selection it displays the actual single date, such as 5 август, or date range, such as 1–7 август.
- (decision) For MVP, the Mahalla filter supports Барча маҳаллалар or exactly one selected Mahalla. Do not add arbitrary multi-Mahalla selection; applying the selection updates all five Lanes and all five statistics together.
- (decision) Use a multi-select Lane visibility/filter control labelled Йўналишлар: N/5 with five fixed checkbox options. Default to all five selected, allow any combination of one or more Lanes, provide Барчасини кўрсатиш, and prevent a zero-Lane state. Show only selected Lane columns and update statistics to the union of Topics in those Lanes; count one canonical multi-Lane Topic once in the overall unique Topic total even when it appears in multiple selected columns. Do not add saved filter sets or custom categories in MVP.
- (decision) Run ordinary plain-text search automatically after about a 400 ms typing pause. Search Topic summaries, Accepted Evidence text, Telegram usernames, and display names within the active date, Mahalla, and explicitly selected Lane filters; do not perform AI semantic search. Search filters Topic cards but never changes Lane selection or hides a selected Lane. A selected Lane with no matches remains visible with the neutral empty state Мос мавзу топилмади. Update statistics to the filtered Topic result, and provide a clear control that restores the unsearched result set.
- (decision) During active plain-text search, explain each result only as needed. If the visible Topic summary contains the query, highlight the matching text without adding content. If the Topic matched only inside Accepted Evidence, a Telegram username, or a display name, add one short temporary match-context line such as Далилда топилди or Фойдаланувчида топилди, preserving original evidence text and script. Remove this context when search is cleared; this is the only search-state exception to compact evidence-preview-free Topic cards.
- (decision) Keep all five statistics cards read-only and non-focusable. Do not use them as implicit filter shortcuts or apply button/hover affordances; the toolbar remains the single predictable place for date, Mahalla, Lane, and search filtering.
- (decision) Display the complete cautious Topic summary on every normal Topic card without line clamping, ellipsis, or per-card expand controls. Keep summaries concise through the AI-output contract, ideally one clear sentence; allow card height to vary rather than risk hiding uncertainty or meaning.
- (decision) Remove prototype-only AI-generated subcategory tags such as Газ босими, Кўча ёритиш, or Сув сифати from MVP Topic cards. Lane placement and the cautious summary provide the approved classification context; do not introduce an undefined subcategory taxonomy, classification behavior, evaluation scope, or related filters.
- (decision) Display Topic latest meaningful activity in Asia/Tashkent using exact PRD formats. For Бугун, Кеча, or one selected date, show 24-hour HH:mm because the active date supplies context. For a date-range result, show DD.MM.YYYY, HH:mm. Do not use relative timestamps on Topic cards.
- (decision) When processing delay means recent messages may not yet appear, show a persistent inline warning below the toolbar until normal processing returns. Keep existing valid Topics and statistics available, state that recent coverage may be incomplete, include the last successful update time, and do not expose Hokim-facing technical details or assign an unclassified failure to a Lane. Exact warning microcopy remains pending.
- (decision) Use this exact single-sentence delayed-processing warning microcopy: Янгиланиш давом этмоқда — айрим сўнгги хабарлар ҳали кўринмаслиги мумкин (охирги муваффақиятли янгиланиш: 12:55). Substitute the actual last successful Asia/Tashkent HH:mm value at runtime.
- (decision) When the complete active result is empty, keep selected Lane headers visible and show one board-wide state: Бугун ҳозирча мавзулар йўқ for the default unfiltered Today view, or Танланган шартлар бўйича мавзулар топилмади when filters or search are active. With active filters, provide Фильтрларни тозалаш. Show truthful zero or adaptive statistics and never imply that no Topics means services are performing well. When only individual selected Lanes are empty, show Мос мавзу топилмади locally in those Lanes.
- (decision) During initial dashboard load, render structure-matching skeletons for the five statistics, selected Lane headers, and representative Topic-card shapes, with screen-reader status Маълумотлар юкланмоқда. Never show invented counts or example content. Match final geometry to prevent layout shift, keep any motion subtle, and disable skeleton animation under reduced-motion preferences.
- (decision) For progressive history loading, place an explicit Яна кўрсатиш control at the bottom of each Lane. Activating it loads only the next result batch for that Lane, appends without moving existing cards or scroll position, and temporarily shows Юкланмоқда... On failure, keep existing results and show the Lane-local retry state Юклаб бўлмади. Қайта уриниш. Do not use automatic infinite scroll or one global load-more action.
- (decision) Use a right-side evidence drawer on the primary desktop layout. On tablet and phone layouts, open the same evidence content as a full-screen view rather than a cramped side drawer. Closing the evidence view returns to the same selected Lane, Topic, filters, and scroll position.
- (decision) Use a responsive horizontal board for selected Lanes in fixed order. Show all five Lanes on the primary desktop envelope, approximately two fixed-width Lanes on tablet, and one near-full-width Lane on phone. Horizontal scrolling uses lane-aligned settling so the viewport does not stop ambiguously between columns. The Йўналишлар: N/5 control determines which Lanes participate; do not replace the board with one dropdown-selected Lane or vertically stack all Lanes.
- (decision) On phone, use a compact sticky toolbar showing the current date, an expandable search control, and Фильтрлар N where N counts active non-default filters. The filter sheet contains Бугун, Кеча, Сана бўйича, one-or-all Mahalla selection, and multi-select Lane checkboxes. Show freshness as a separate compact status line. Keep the full single-row toolbar on the primary desktop layout; do not horizontally hide all controls or consume the phone viewport with multiple sticky rows.
- (decision) Keep all five read-only statistics accessible on narrower screens through one horizontal card-aligned row. Show approximately two or three cards on tablet and one full card plus part of the next on phone to signal additional content. Preserve filter-aware updates and read-only semantics; do not use a tall two-column grid or hide metrics behind another control.
- (decision) Use calm civic intelligence as the visual posture: trustworthy, restrained, evidence-first, and information-dense without feeling crowded. The product should feel modern but not fashionable, serious but not bureaucratic, and must avoid commercial-dashboard spectacle, gamification, dramatic urgency, performance scoring, or decorative visual noise.
- (decision) Lock only the product name Mahalla Ovozi as an existing brand element. The user does not like the logo shown in the prototype reference; explicitly reject it as a direction. Treat its red accent, iconography, typography, and surrounding styling as non-authoritative and open to evaluation. No separate official brand guide or visual asset is currently confirmed.
- (decision) For MVP, use a text-only Mahalla Ovozi wordmark with deliberate typography and no permanent logo symbol. Do not design a full logo system during this UX phase and do not introduce a temporary generic icon that could become accidental branding; a dedicated logo can be explored later without changing the product experience.
- (decision) Display the in-product text wordmark as Маҳалла Овози in Uzbek Cyrillic so the header matches the Cyrillic-only MVP interface. Retain Mahalla Ovozi as the canonical technical and English project name in repository paths, technical documents, and English discussion.
- (decision) Use one highly legible sans-serif family throughout the wordmark, Topic summaries, statistics, controls, and evidence metadata. Require complete Uzbek Cyrillic support and stable browser rendering. Favor comfortably larger, readable typography over fitting more cards into the viewport; avoid tiny interface text. Exact type sizes remain pending approval.
- (decision) Adopt this baseline type scale, subject only to final visual mock adjustment: wordmark 20/28 px, statistic values 28/34, Lane headings 16/24, Topic summaries 16/24, evidence messages 16/24, toolbar controls 15/22, metadata and state labels 14/20. Use no user-facing text below 14 px. Keep Topic summaries medium or semibold and evidence regular. Preserve sizes on smaller screens, adapt layout instead, and support browser zoom to 200 percent without clipping.
- (decision) Support one light theme only in MVP. Do not implement dark mode without later pilot evidence that justifies the additional design, contrast, state, screenshot, and QA scope. Build the light palette with semantic tokens so a future theme can be added without changing experience rules; light-only does not require harsh pure-white surfaces.
- (change) Skip the HTML color-theme comparison and external handoff/prototype step. Proceed directly with one recommended light-theme direction; the user will evaluate it later through a future prototype and may retain or revise it then.
- (decision) Use Civic Teal as the provisional recommended light-theme baseline for subsequent UX work: page #F5F7F6, raised #FFFFFF, subtle #EDF3F1, primary text #172321, secondary text #52615E, border #C9D5D1, primary action and selection #0F5C5E, focus #007A7C. Use restrained lane pairs: Hokim #3F4B7A on #F0F1F8, Water #1F6688 on #EAF4F8, Electricity #765300 on #FBF3DF, Gas #81462F on #F7EEE9, Waste #356848 on #EAF3EC. Warning uses #6B4B00 on #FFF4D6 and search match #F5DD77 with #172321 text. Preserve explicit text labels and never depend on color alone. Verified pairs meet WCAG AA for normal text; this remains revisable after later prototype review.
- (decision) Use a restrained bordered component language: 8 px corner radius for cards and standard controls, thin semantic borders for separation, and no persistent card shadows. Reserve subtle elevation shadows for temporary or overlaid surfaces such as the evidence drawer, dropdowns, filter sheets, and modals. Communicate selected and focused states through explicit semantic borders and focus rings rather than decorative elevation.
- (decision) Use a 4 px base spacing grid with primary steps of 8, 12, 16, 24, and 32 px. Default Topic-card internal padding to 16 px and vertical gaps between Topic cards to 12 px. Apply the scale consistently across toolbar groups, statistics, Lane headers, cards, drawers, and responsive layouts; preserve the approved readable type sizes instead of compressing typography to increase density.
- (decision) Use restrained semantic color in a consistent outline-icon system rather than making every icon monochrome. Lane icons inherit their approved lane accent color; general action icons use neutral text color or primary teal; warning and state icons use their corresponding semantic state color. The same concept or action keeps the same icon and color across surfaces. Pair icons with text whenever meaning may be unclear, and never rely on icon color alone. Avoid arbitrary per-icon colors, decorative multicolor illustration, and reuse of the prototype icon styling by default.
- (decision) Use restrained functional motion with approximately 120–180 ms transitions for evidence-drawer opening, filter expansion, focus and selection changes, and newly revealed content. Avoid card lifting, bounce, pulsing alerts, parallax, and decorative animation. Honor prefers-reduced-motion by disabling nonessential movement; maintain immediate and clear state feedback without depending on animation.
- (decision) Keep the UX specification implementation-neutral because no frontend or established UI component system is currently present. Define semantic visual tokens, component anatomy, behavior, interaction states, responsive rules, and accessibility requirements without locking the product to shadcn, MUI, Ant Design, or a specific icon package. Defer the technical UI foundation and icon-library choice to architecture or implementation, requiring it to preserve the approved UX contracts.
- (decision) Write all MVP interface microcopy in calm, concise, factual Uzbek Cyrillic. State what happened and what is affected without blame, drama, exclamation marks, fake reassurance, bureaucratic phrasing, or Hokim-facing technical details. Provide a clear next action only when the user can perform a useful recovery or navigation action, such as Қайта уриниш or Фильтрларни тозалаш; do not add an action when waiting is the only truthful option. Preserve original Telegram evidence verbatim and apply product voice only to interface copy and cautious derived summaries.
- (decision) Use predictable keyboard interaction: Tab follows the visual and DOM order through toolbar controls and Topic cards; Enter or Space opens a focused Topic; Escape closes the evidence view and returns focus to its originating Topic card. Keep the desktop evidence drawer non-modal so the dashboard remains reachable. Treat the phone full-screen evidence view as modal and contain focus until it closes. Do not implement custom arrow-key board navigation for MVP without later usability evidence.
- (change) Expand this UX package from the deeply specified Hokim dashboard to full MVP surface coverage from the PRD, including Product Owner Console, District Setup and activation, System Health, access/sign-in, and required cross-surface navigation. Preserve the approved dashboard decisions and add the missing journeys, screens, states, interactions, and accessibility rules before finalization.
- (decision) Use a persistent left sidebar for the Product Owner Console sections required by the PRD, with a clearly visible District-context selector in the Console header. Preserve the selected District while navigating between applicable sections. Permit an all-Districts context only on aggregate surfaces such as Overview, System Health, District lists, and subscription summaries. Require one explicit District before displaying retained Topics or evidence, credentials, Telegram mappings, Hokim accounts, District configuration, or destructive District actions; never combine evidence from multiple Districts in one result view. On narrower screens, collapse the sidebar into an accessible navigation drawer while keeping the current District context visible.
- (decision) Structure District onboarding as a resumable setup checklist rather than a rigid linear wizard. The District setup overview shows District identity, Subscription, Telegram bot, group-to-Mahalla mappings, Hokim account, required validation checks, and Activation with explicit incomplete, passed, or failed states. Each checklist item opens the relevant permanent Console section for editing, allows completion in any practical order, preserves partial work as Setup incomplete, and shows remaining blockers beside a disabled Activation control. Enable activation only after every PRD-required check passes; show actionable failure reasons and audit attempts.
- (decision) Use one private username-and-password sign-in page for both Product Owner and Hokim accounts with no role selector. Route a successfully authenticated Product Owner to Console Overview and a Hokim to the assigned District dashboard; when an authorized protected deep link initiated sign-in, return to that target. On session expiry, return to sign-in with a neutral explanation. Use one generic invalid-credentials response that does not disclose account existence. Explain Suspended-District access as temporarily unavailable due to subscription state rather than technical failure. Deny wrong-role and cross-District requests without revealing protected information.
- (decision) When creating or resetting the single Hokim account, generate a strong temporary password and display it once with a copy control; never redisplay it. The Product Owner delivers it through a trusted external channel. Require the Hokim to replace it at first sign-in with a password of at least 12 characters. Afterward, show only credential status and the last-reset time in the Console. Resetting, disabling, or replacing the account immediately revokes all existing sessions; the MVP provides no email, SMS, or self-service recovery.
- (decision) Structure district-scoped Telegram Setup as a bot connection card plus a searchable Mahalla mapping table. The bot card shows validation state and last successful check with Replace and Remove actions, but never redisplays the stored token. Each mapping row shows Mahalla, approved group, enabled state, bot access, Group Privacy Mode check, ordinary non-command test-message receipt, and overall mapping status. Add or edit a mapping in a focused side panel; after configuration, show an explicit waiting state for the test message with timeout guidance and retry. Prevent one-to-one mapping conflicts before save, state that changes affect future intake only, and use stacked mapping cards instead of a compressed table on narrow screens.
- (decision) Use risk-proportional confirmation for Telegram changes. Stage bot replacement by validating the new token while the current bot remains active, then explicitly confirm the swap. Require confirmation before bot removal and before disabling, removing, or remapping an active group; show the exact District, group, Mahalla, future-intake consequence, and any activation impact. Do not add an extra confirmation for creating a new mapping or correcting an incomplete inactive mapping. State that retained evidence and historical District/Mahalla attribution remain unchanged, and surface the audit result for successful or failed changes.
- (decision) Structure System Health in two levels: a main surface with overall service status, an all-District active-issue list, and a District-by-component status matrix; selecting a District narrows the matrix while preserving District context. Open issue details in a drawer showing severity, exact health state, affected component and District, impact, start time, latest check, privacy-safe identifiers, beginner-friendly explanation, and recommended next step. Expose Retry only for eligible incomplete work and never for completed results. Link subscription-caused pauses to Subscriptions rather than classifying them as technical failures. Never reveal resident message content in System Health; link authorized investigation to the separate District-scoped evidence browser. Keep Quiet and Unknown explicit and distinct from Healthy or Unavailable.
- (decision) Place the Product Owner operational browser inside each District detail as Topics and Evidence rather than adding another top-level Console section. Require one explicit District context and clear results plus any open detail before switching Districts. Use an operational result list with date or range, Mahalla, Lane, and plain-text filters; show Topic summary, Topic day, Mahalla, all Lane memberships, latest activity, evidence count, and configuration version. Load large retained result sets progressively. Open a right-side detail drawer with the full chronological evidence trail, attribution, processing lineage, privacy-safe trace identifiers, and original evidence text and script. Keep the Hokim five-Lane board optimized for situational awareness and this Console view optimized for troubleshooting.
- (decision) Present subscriptions through an all-District summary list plus a District detail view. The detail view leads with current lifecycle state, exact state-start time, operational consequences, and next scheduled transition, followed by a lifecycle timeline for Grace expiry, cancellation, live-deletion deadline, recovery eligibility, live-deletion verification, and protected-backup expiry verification when applicable. Show only actions valid for the current state: Start Grace, Restore Active, Cancel, or Start Recovery. Before confirmation, preview exact effects on intake, processing, Hokim access, retained data, and missed-message backfill. Keep external payment reference and internal note secondary and provide no payment or invoice UI. After final deletion, expose the minimal permanent deletion proof through Audit History rather than a deleted District page.
- (decision) Use a high-assurance District-cancellation confirmation. Show the exact District name and current state, cancellation time, live-deletion deadline, protected-backup expiry deadline, immediate stop to intake, processing, and Hokim access, bot-token removal, absence of missed-message backfill, and the recovery deadline. Require a short cancellation reason for Audit History and require typing the District name before enabling the explicit Cancel District action. Use ordinary consequence-confirmation dialogs for Start Grace, Restore Active, and Start Recovery; reserve typed confirmation for cancellation.
- (decision) Structure AI Operations as a versioned draft-and-activation workflow with separate Global Settings and District Settings. Show the current active version, activation time, and non-secret summary. Editing creates a draft with validation and a field-level diff against the active version; it never changes production immediately. Before activation, state that the change affects future processing only, leaves committed decisions unchanged, and performs no replay. Require a short change reason and record activation in Audit History. Keep version history read-only. Implement rollback by creating and activating a new version copied from the selected earlier version, never by deleting or rewriting history.
- (decision) Classify risky AI configuration changes deterministically from the changed fields rather than through AI judgment. Use elevated confirmation for model changes, prompt changes, global service-vocabulary changes, rollback, and removing or replacing District recognition terms. Show the field-level diff, affected global or District scope, future-only effect, and required change reason before activation. Adding a new District-local vocabulary term follows normal activation confirmation. If a draft combines ordinary and risky changes, use the elevated flow for the complete activation. Keep every activation versioned and audited.
- (decision) Present Audit History as a read-only chronological table with District, action type, actor, timestamp, and short outcome. Provide District, action-type, and date filters plus free-text search over actor, action, supplied reason, privacy-safe identifiers, and non-secret old/new values. Never search or display credentials, bot tokens, resident evidence text, or redacted secret values. Open the complete immutable audit record in a detail drawer and load long histories progressively. Keep permanent District-deletion proofs in a clearly labelled subsection searchable by deleted District name or identifier and date. Provide no edit or delete actions.
- (decision) Use Console Overview as an all-District operational summary rather than an analytics dashboard. Show overall service health and last-check time, then list Districts in a stable alphabetical order with separate setup, subscription, and technical-health states, active-issue count, and nearest scheduled lifecycle event when one exists. Preview current Critical and Warning issues with links to System Health, and route management work to the appropriate dedicated Console section. Do not show resident evidence text, cross-District rankings or benchmarking, charts, AI scores, direct destructive actions, or duplicate management forms on Overview.
- (decision) Apply one Console-wide responsive contract without creating a separate mobile product. On desktop, keep the persistent sidebar, full data tables or matrices, and right-side detail drawers. On narrower screens, keep District context visible and use the approved navigation drawer. Transform ordinary tables into stacked record cards without hiding required fields or actions; retain horizontally scrollable comparison views such as System Health matrices and AI configuration diffs when column comparison is essential, keeping the identifying column visible. Reflow forms to one column, open side panels and detail drawers as full-screen views, and restore the same filters, record context, and scroll position when they close. Collapse dense filter rows into a Фильтрлар N control and filter sheet. Never shrink approved typography, clip Uzbek Cyrillic, or remove important capabilities merely to fit a smaller viewport.
- (decision) Use predictable native focus and keyboard behavior throughout the Product Owner Console. Tab follows visual and DOM order, and only interactive controls receive focus. Give each record row or card one clear focusable primary control for opening details while keeping secondary actions separate; Enter or Space activates the focused control. Keep read-only desktop detail drawers non-modal: move focus into the opened drawer while leaving the Console reachable. Treat edit panels, confirmation dialogs, and narrow-screen full-screen views as modal and contain focus within them. Escape closes the topmost surface and returns focus to its originating control. Initially focus the safe cancel action in ordinary confirmation dialogs and the required District-name field in typed District cancellation. Enter must never confirm a destructive action automatically. Use native keyboard patterns and add no custom table or grid arrow navigation in MVP.
- (decision) Use explicit saving for Console configuration forms and do not auto-save. Provide a Сақлаш action, validate fields as they are completed and again on save, and preserve valid entered values when another field fails. Show Сақланмаган ўзгаришлар while a form differs from its saved state. If the user closes or leaves with unsaved changes, ask whether to continue editing or discard them. Save District setup one checklist section at a time; a successful save updates the checklist and supports later resumption from that incomplete section. Persist drafts only where already required for District setup and AI Operations, not for every ordinary Console form. Saving configuration never activates it or performs a lifecycle change; activation and lifecycle actions remain separate confirmed operations.
- (decision) Use truthful, recoverable operation feedback throughout the Console. Do not show optimistic success for configuration, account, Telegram, subscription, AI activation, retry, or deletion actions. While an action runs, show progress on its initiating control and prevent duplicate submission without freezing the whole page. After an ordinary save, update the visible saved state and show a nearby Сақланди confirmation. After a sensitive or lifecycle action, show a persistent result containing the District, confirmed new state, timestamp, and an Audit History link when available. On validation failure, show field-level errors plus a focused summary at the form start. On operational failure, keep the form or dialog open, preserve entered values and context, explain what failed, and offer retry only when safe. Temporary toast notifications may supplement feedback but are never the only confirmation. Announce progress and results to assistive technology. Do not add a global notification center or background-job interface in MVP.
- (decision) Use one truthful Console-wide data-state pattern. On initial loading, show structure-matching skeletons without invented values and announce loading to assistive technology. During background refresh, keep the last successfully loaded data, active filters, selected record, open drawer, and scroll position. If refresh fails, keep that data visible and show a persistent stale-data warning with the last successful update instead of clearing the screen. Distinguish an empty source from a filtered result with no matches, and offer Фильтрларни тозалаш only for the filtered case. A section-level failure preserves Console navigation and District context, explains what failed, and offers Қайта уриниш only when safe; a failure limited to one record or component remains local rather than replacing the page. Use the approved explicit Яна кўрсатиш progressive-loading pattern for long lists and never automatic infinite scroll. Empty or quiet data never implies healthy service.
- (decision) Keep Hokim dashboard Help and profile controls minimal. Ёрдам opens a concise help drawer on desktop and the same content full-screen on smaller devices. Explain that Topics are cautious signals rather than verified facts, one Topic may appear in multiple Lanes, the meanings of Янги and Янгиланди, chronological evidence and best-effort Telegram navigation, freshness and delay warnings, the 90-day retention boundary, and that Mahalla Ovozi neither recommends nor tracks the Hokim decision. Opening or closing Help preserves dashboard filters, Lane positions, and evidence context. The profile menu shows the authenticated username, role, and the Hokim fixed District; its only ordinary action is Чиқиш, which requires no confirmation. Keep first-sign-in password replacement as a separate required flow. Do not add profile editing, self-service recovery, support chat, feedback forms, or an AI help assistant in MVP.
- (decision) Stale data may remain visible only while the current session, role, District authorization, subscription access, and retention status remain valid. Session expiry, account revocation or replacement, Suspension, Cancellation, retention expiry, or completed deletion must immediately remove protected content and show the appropriate sign-in, denied, expired, or deleted state.
- (decision) Changing District is an atomic context transition. After any required unsaved-change decision, immediately clear all prior-District content-bearing state, including search text, content-derived filters and labels, counts, selections, open details, and errors, before loading the new District. Cancel or ignore earlier in-flight work, and never render a response whose District context no longer matches the currently selected District.
- (decision) Plain-text search text that may contain Accepted Evidence or resident identity is ephemeral active-session UI state only. Never place it in shareable URLs, browser history, saved or recent-search suggestions, persistent browser storage, Audit History, analytics, telemetry, or routine logs. Clear it immediately on sign-out, session expiry, permission loss, or District change.
- (decision) District onboarding must include a required checklist confirmation that the existing customer arrangement disclosed the Product Owner's standing operational access to that District's retained Topics and Accepted Evidence. This confirms an external disclosure; it is not legal advice or an in-product consent workflow. Activation remains blocked until confirmation. Audit only the District, confirming actor, and confirmation time, never resident content.
- (decision) On the existing first-sign-in password-replacement screen, show a concise factual notice that the Product Owner has standing operational access to the District's retained Topics and Accepted Evidence for operation and troubleshooting under the customer arrangement. The notice is informational, not permission or consent: add no agreement checkbox, separate acceptance record, or additional product-access gate beyond the required password replacement.
- (decision) Across all Hokim and Product Owner evidence surfaces and search-match contexts, show the Telegram username when available; otherwise show the display name. Never infer, index for search, search by, or display a phone number.
- (decision) Every user-visible failure across the Hokim dashboard and Product Owner Console, including field, record, section, action-result, toast, and System Health errors, must use sanitized information. Show only a safe error category, affected scope, time, privacy-safe identifier, and a useful next action when one exists. Never display or copy raw resident content, credentials, bot tokens, provider keys, other secrets, or raw upstream error bodies.
- (decision) A generated temporary password appears only on a dedicated one-time credential surface immediately after account creation or reset. Copy is available only there. Closing or dismissing the surface, navigating away, using browser Back or Forward restoration, or reloading makes the password unavailable. Persistent UI shows only credential status and the last-reset time. The password must never enter URLs, browser storage or restored page state, autofill, errors, telemetry, routine logs, or Audit History. If it is lost, the Product Owner must generate a new temporary password through reset.
- (decision) Suspension and Cancellation immediately deny the District Hokim's product access, but do not lock the Product Owner out of the operational controls needed to manage that lifecycle. The authenticated Product Owner retains access to permitted Console surfaces for subscription management, recovery and setup, deletion status, System Health, Audit History, and retained District Topics and Accepted Evidence while that content remains authorized, unexpired, and not deleted. This access never resumes intake, AI processing, or Hokim access.
- (decision) Normal retention continues unchanged during Suspension, Cancellation, and recovery setup. A Topic and all its Accepted Evidence expire together 90 days after the Topic's latest relevant evidence timestamp. These lifecycle states never pause or extend retention, and recovery can restore only content that remains unexpired.
- (decision) Cancellation reasons, AI-configuration change reasons, subscription notes, and other audit-bound free-text fields are non-sensitive operational metadata only. Their labels and help text must prohibit resident message text, resident identifiers, credentials, bot tokens, provider keys, and other secrets. If a known product secret is detected, reject the save with a sanitized field error. Do not add a general personal-data-redaction workflow to MVP.
- (decision) AI Operations configures future production behavior only. Manual MVP AI validation remains an external Product Owner activity using controlled non-real messages through the ordinary District onboarding, Telegram integration, processing, dashboard, and evidence flows. Do not add a special validation surface, persisted validation artifacts, AI score, or formal evaluation report.
- (decision) Keep {colors.border} (#C9D5D1) only for decorative separators. Add {colors.boundary-essential} as #6B7C77 for input, button, selectable-card, and other boundaries required to identify a control or region. Essential boundaries must maintain at least 3:1 contrast against every adjacent surface. Focus and selection remain separate semantic states and must still be distinguishable. Verified contrast is 4.40:1 against raised white, 4.09:1 against the page surface, and 3.91:1 against the subtle surface.
- (decision) A desktop read-only detail drawer is a labelled non-modal complementary region, never a modal dialog. Opening it moves focus to its heading; a clearly labelled Close control is the first operable control. The opener exposes a programmatic expanded and control relationship to the drawer. Close or Escape returns focus to the exact originating Topic or record control. The drawer sits beside its owning surface in DOM order. The underlying surface remains operable, but no focusable target may be visually covered by the drawer; the layout must reserve space or temporarily remove covered targets from keyboard navigation.
- (decision) Narrow-screen read-only Evidence, issue, audit, record, Help, and mapping details are full-screen pages, never modal dialogs. On entry, focus moves to the main heading; Back or Close returns to the exact originating control and restores filters, scroll, and record context. Edit panels, confirmation dialogs, and navigation or filter sheets are modal overlays. Each modal has a programmatic title, an accessible consequence description when relevant, an inert background, contained keyboard focus, a visible Close or Cancel control, Escape dismissal, and exact focus restoration. Enter alone never confirms a destructive action.
- (decision) Keep native Tab order on the Lane board and add no custom arrow-key navigation. Whenever all five Lanes do not fit, expose the board as a labelled horizontal scroll region with visible, keyboard-operable Previous Lane and Next Lane controls. Each control moves one Lane at a time and announces the newly visible Lane name and position. When Tab focuses an off-screen Lane or Topic card, bring it fully into view. Preserve the board's horizontal position and each Lane's vertical position when opening and closing details. Under reduced motion, scrolling is immediate and not animated.
- (decision) Responsive behavior follows the effective CSS viewport, including browser zoom, rather than the physical screen or device type. The five-Lane desktop composition applies only when the effective viewport supports it. At 200% zoom, activate tablet or phone patterns as needed, including compact filters, collapsed Console navigation, fewer visible statistic cards, horizontal Lane navigation, one-column forms, and the appropriate full-screen detail or edit treatment. Prevent page-level horizontal overflow; only intentional labelled Lane-board, matrix, or diff regions may scroll horizontally. At a 320 CSS-pixel equivalent viewport, keep all core content and actions usable without clipped Cyrillic, overlapping sticky regions, or hidden controls.
- (decision) Genuinely tabular desktop data uses semantic tables with programmatically associated headers. When a table becomes stacked cards, every visible value repeats its field label, reading order remains logical, and each card retains one clearly labelled primary opener. Comparison-essential matrices and diffs remain inside a labelled, keyboard-scrollable region with brief scrolling instructions; the identifying column remains visually visible, and every data cell is programmatically associated with both its row identity and column header. Preserve sort, filter, selected-record, and scroll state across viewport, browser-zoom, and orientation changes.
- (decision) During entry or blur validation, keep focus in the current field and never let errors steal focus. After a failed Save, focus one error summary at the start of the form; it shows the error count and links to every invalid field. Programmatically mark each invalid control as invalid and associate it with its specific error and help text. Announce each newly appearing error once without repeated announcements, preserve every valid entered value, and move focus to an invalid field only when the user activates its summary link.
- (decision) A dirty form must not be silently discarded by District switching, Console navigation, Close or Escape, browser Back, sign-out, or responsive surface replacement. Before the user decides, keep the current District, form values, open detail, and focus origin unchanged; do not begin the requested transition or clear protected state. Continue editing cancels the transition and restores the exact prior context. Discard clears the draft and then performs the requested transition. A successful Save allows the transition to continue. Viewport, zoom, or orientation reflow preserves the dirty form automatically without prompting; if a surface replacement cannot preserve it, apply the same guard before replacement.
- (decision) Use scoped announcements for the affected page, Lane, form, or action, not one global live region. Use polite atomic status announcements for user-initiated loading, settled search or filter result counts, save progress, and successful results. Use assertive alerts only for blocking failures or lost availability requiring immediate attention. Do not announce unchanged background refreshes. When refreshed content changes, announce one concise combined new or updated count. Announce Яна кўрсатиш completion or failure only for its affected Lane or list. Never announce individual skeleton items. Deduplicate identical messages and prevent stale or overlapping announcements.
- (decision) On phone and tablet, primary controls have a minimum 44 by 44 CSS-pixel activation area. Icon-only controls meet the same minimum at every viewport width. Adjacent compact targets have at least 8 CSS pixels of separation. A visible icon may be smaller only when its padded activation area meets the minimum and does not overlap another action. Apply this to drawer Close, filter controls, Previous Lane and Next Lane, temporary-password Copy, and per-evidence Telegram actions.
- (decision) Acceptance-test the implementation font stack for the complete Uzbek Cyrillic alphabet, including Ў ў Қ қ Ғ ғ Ҳ ҳ, across every supported browser and OS family. Labels, buttons, chips, and other controls may grow or wrap; fixed heights must never clip glyphs or move actions outside their containers. Long District and Mahalla names and status labels wrap safely without losing meaning. Prose and evidence wrap normally while original evidence line breaks remain preserved. Long unbroken technical identifiers use safe breaking while keeping the complete value available to visual and assistive-technology users. Mixed-script evidence uses appropriate glyph-capable font fallbacks.
- (decision) Under prefers-reduced-motion: reduce, drawer, sheet, filter-expansion, and newly revealed-content transitions become immediate state changes. All programmatic scrolling, including focus scrolling and Lane navigation, occurs immediately without smooth animation. Lane alignment may snap directly to its destination but must never animate across the board. Skeleton animation remains disabled. Focus rings, selected states, progress text, and other essential feedback remain fully visible as static feedback.
- (decision) Under Key Flows, add a compact Requirement Coverage table that indexes every exact PRD requirement ID and name (FR-1–FR-32 and NFR-1–NFR-8) to the carrying UJ-1–UJ-5 flow or flows. Each requirement appears exactly once as a table row; use multiple flows only when materially applicable. Do not duplicate requirement prose; the PRD remains the source. Add no new journey because every interaction-bearing requirement resolves to UJ-1–UJ-5; map backend constraints to the flow whose result or failure handling depends on them. Required anchors: FR-15 maps to UJ-1 and UJ-5; FR-27 to UJ-2; FR-32 to UJ-4 with failure visibility in UJ-2; NFR-7 to UJ-1–UJ-5.
- (decision) Apply one browser network-loss contract to every surface. If previously loaded content remains permitted by the existing security, lifecycle, and retention precedence rules, keep it visible read-only with a persistent Uzbek Cyrillic offline warning and last successful update time. Block new loads and all network-dependent mutations, including saves, retries, configuration, credential, lifecycle, and destructive actions; never queue, automatically resubmit, or claim success for them. If no usable content was loaded, preserve known shell/context and show a connection-unavailable state with manual Retry. On reconnect, revalidate the session, role, District authorization, subscription/lifecycle, and retention before refreshing or restoring actions; preserve viewing context only when access remains valid, and require explicit retry for any unconfirmed action. Browser connectivity loss alone must not create a System Health issue or imply that the product, server, or District is unavailable.
- (decision) Resolve Topic-summary font-weight ownership by keeping typography.topic-summary.fontWeight at 600 (semibold) and making that token the sole authority. Remove the body-table alternative that permits medium or semibold. Do not change the Topic-summary font size, line height, or any other typography value.
- (decision) Resolve selection-token ownership without adding or changing color values. Use colors.primary as the sole selection color as well as the primary-action color, and reserve colors.focus only for visible keyboard focus. Change topic-card.selected-border to colors.primary. When a selected item also has keyboard focus, show the selected boundary together with a separate focus ring so both states remain distinguishable.
- (decision) Remove minor token/prose duplication without changing any token, layout, or behavior. Keep frontmatter tokens as the sole source for role-specific typography sizes, line heights, weights, and spacing values. Replace the Typography table numeric columns with a short usage column while retaining the explicit 14 px accessibility floor as a behavioral requirement. Remove the repeated 4 px base-grid literal from Layout & Spacing and reference the spacing tokens only.
- (decision) Treat the Telegram bot token as an active-transaction secret. Its raw value may exist only during the current secret-entry and validation transaction. Exclude it from resumable District-setup drafts, URLs, browser history, persistent browser storage, restored page state, autofill, analytics, telemetry, routine logs, errors, and Audit History. Clear it after successful server-side storage, dismissal or navigation, reload, or District change. Persist only non-secret validation and status metadata, and require token re-entry whenever the transaction is interrupted.
- (decision) When the five-card statistics strip does not fit the effective viewport, keep metric cards read-only and non-focusable and show visible keyboard-operable Previous statistic and Next statistic controls beside a labelled statistics region. Each activation moves one statistic at a time, announces the newly visible metric name and position, and preserves the current statistic position across viewport, zoom, and orientation changes. Under reduced motion, movement is immediate and not animated.
- (decision) On phone and tablet, every interactive control, including primary, secondary, text-labelled, link-style, and icon-only actions, must provide at least a 44 by 44 CSS-pixel activation area. Keep at least 8 CSS pixels between adjacent compact targets and never allow padded activation areas to overlap. Visible text or icons may remain compact inside the compliant activation area. Icon-only controls retain the same minimum at every viewport width.
- (decision) Remove the new statistics-navigation and touch-target wording duplication without changing behavior or token values. Keep the complete statistics-navigation contract in the metric-card component row and the complete touch-target contract in the action-control component rows, using targets.touch-min and targets.compact-gap as the value owners. Interaction Primitives, Accessibility Floor, and Responsive and Platform must reference those component owners and retain only section-specific consequences.
- (event) Finalized DESIGN.md and EXPERIENCE.md after definitive rubric, accessibility/responsive, and privacy/evidence validation returned zero findings, followed by structural editorial review and prose review with no substantive or editorial changes recommended. Both spines are status final, dated 2026-08-11. No prototype, mockup, theme comparison, commit, or scope expansion was created.
